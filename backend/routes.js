const express = require('express');
const router = express.Router();
const bodyParser = require('body-parser')


// YOUR API ROUTES HERE

// SAMPLE ROUTE
// router.use('/labelData', (req, res) => {
//     res.json({ success: true });
// });

module.exports = router;
